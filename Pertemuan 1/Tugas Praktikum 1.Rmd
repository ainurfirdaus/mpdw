---
title: '[MPDW] Tugas Praktikum 1'
author: "Ainur Roudlotul Jannah Firdaus"
date: "2026-08-25"
output: rmdformats::readthedown
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Impor Data

```{r}
library("forecast")
library("graphics")
library("TTR")
library("TSA")
library("readxl")
library("ggplot2")
```

```{r}
data <- read_excel("D:\\SMT 5\\Metode Peramalan Deret Waktu\\Tugas\\Tugas Praktikum 1\\100 Gugus Data Kelompok.xlsx")
head(data)
```

# Eksplorasi Data

```{r}
View(data)
str(data)
```

```{r}
dim(data)
```

```{r}
data.ts <- ts(data$`Monorail _User`)
data.ts
```

```{r}
summary(data.ts)
```

```{r}
ts.plot(data.ts, xlab="Date ", ylab="Monorail User", 
        main = "Time Series Plot")
points(data.ts)
```

# Pembagian Data

```{r}
colnames(data)[2] <- "Monorail_User"

n <- nrow(data)
n_train <- floor(0.8 * n)

training_ma <- data[1:n_train, ]
testing_ma  <- data[(n_train+1):n, ]

training_ma$date <- as.Date(training_ma$Date)
testing_ma$date <- as.Date(testing_ma$Date)

train_ma.ts <- ts(training_ma$Monorail_User)
test_ma.ts  <- ts(testing_ma$Monorail_User)
```

```{r}
plot(data.ts, col="red",main="Plot semua data")
points(data.ts)
```

```{r}
plot(train_ma.ts, col="blue",main="Plot data latih")
points(train_ma.ts)
```

```{r}
plot(test_ma.ts, col="blue",main="Plot data uji")
points(test_ma.ts)
```

```{r}
ggplot() + 
  geom_line(data = training_ma, aes(x = date, y = Monorail_User, col = "Data Latih")) +
  geom_line(data = testing_ma, aes(x = date, y = Monorail_User, col = "Data Uji")) +
  labs(x = "Periode Waktu", y = "Rail_Monorail", color = "Legend") +
   scale_colour_manual(name="Keterangan:", breaks = c("Data Latih", "Data Uji"),
                      values = c("blue", "red")) + 
  theme_bw() + theme(legend.position = "bottom",
                     plot.caption = element_text(hjust=0.5, size=12))
```

# Single Moving Average

## Pemulusan

```{r}
data.sma<-SMA(train_ma.ts, n=4)
data.sma
```

```{r}
data.ramal<-c(NA,data.sma)
data.ramal 
```

```{r}
data.gab<-cbind(
  aktual=c(data.ts),
  pemulusan=c(data.sma,rep(NA,24)),
  ramalan=c(data.ramal,rep(data.ramal[length(data.ramal)],23)))

data.gab
```

```{r}
ts.plot(data.ts, xlab="Date ", ylab="Monorail User", main= "SMA N=4 Data Sales")
points(data.ts)
lines(data.gab[,2],col="green",lwd=2)
lines(data.gab[,3],col="red",lwd=2)
legend("topleft",c("data aktual","data pemulusan","data peramalan"), lty=8, col=c("black","green","red"), cex=0.5)
```

## Evaluasi

### Data Latih

```{r}
error_train.sma = train_ma.ts-data.ramal[1:length(train_ma.ts)]

SSE_train.sma = sum(error_train.sma[5:length(train_ma.ts)]^2)
MSE_train.sma = mean(error_train.sma[5:length(train_ma.ts)]^2)
MAPE_train.sma = mean(abs((error_train.sma[5:length(train_ma.ts)]/train_ma.ts[5:length(train_ma.ts)])*100))

akurasi_train.sma <- matrix(c(SSE_train.sma, MSE_train.sma, MAPE_train.sma))
row.names(akurasi_train.sma)<- c("SSE", "MSE", "MAPE")
colnames(akurasi_train.sma) <- c("Akurasi m = 4")
akurasi_train.sma
```

### Data Uji

```{r}
error_test.sma = test_ma.ts-data.gab[81:100,3]

SSE_test.sma = sum(error_test.sma^2)
MSE_test.sma = mean(error_test.sma^2)
MAPE_test.sma = mean(abs((error_test.sma/test_ma.ts*100)))

akurasi_test.sma <- matrix(c(SSE_test.sma, MSE_test.sma, MAPE_test.sma))
row.names(akurasi_test.sma)<- c("SSE", "MSE", "MAPE")
colnames(akurasi_test.sma) <- c("Akurasi m = 4")
akurasi_test.sma
```

# Double Moving Average

## Pemulusan

```{r}
dma <- SMA(data.sma, n = 4)
At <- 2*data.sma - dma
Bt <- 2/(4-1)*(data.sma - dma)
data.dma<- At+Bt
data.ramal2<- c(NA, data.dma)

t = 1:20
f = c()

for (i in t) {
  f[i] = At[length(At)] + Bt[length(Bt)]*(i)
}

data.gab2 <- cbind(aktual = c(data.ts), 
                   pemulusan1 = c(data.sma,rep(NA,20)),
                   pemulusan2 = c(dma, rep(NA,20)),
                   At = c(At, rep(NA,20)), 
                   Bt = c(Bt,rep(NA,20)),
                   ramalan = c(data.ramal2, f[-1]))
data.gab2
```

```{r}
ts.plot(data.ts, xlab="Date", ylab="Monorail User", main= "DMA N=4 Data Sales")
points(data.ts)
lines(data.gab2[,3],col="green",lwd=2)
lines(data.gab2[,6],col="red",lwd=2)
legend("topleft",c("data aktual","data pemulusan","data peramalan"), lty=8, col=c("black","green","red"), cex=0.8)
```

## Evaluasi

### Data Latih

```{r}
error_train.dma = train_ma.ts-data.ramal2[1:length(train_ma.ts)]

SSE_train.dma = sum(error_train.dma[8:length(train_ma.ts)]^2)
MSE_train.dma = mean(error_train.dma[8:length(train_ma.ts)]^2)
MAPE_train.dma = mean(abs((error_train.dma[8:length(train_ma.ts)]/train_ma.ts[8:length(train_ma.ts)])*100))

akurasi_train.dma <- matrix(c(SSE_train.dma, MSE_train.dma, MAPE_train.dma))
row.names(akurasi_train.dma)<- c("SSE", "MSE", "MAPE")
colnames(akurasi_train.dma) <- c("Akurasi m = 4")
akurasi_train.dma
```

### Data Uji

```{r}
error_test.dma = test_ma.ts-data.gab2[81:100,6]
SSE_test.dma = sum(error_test.dma^2)
MSE_test.dma = mean(error_test.dma^2)
MAPE_test.dma = mean(abs((error_test.dma/test_ma.ts*100)))

akurasi_test.dma <- matrix(c(SSE_test.dma, MSE_test.dma, MAPE_test.dma))
row.names(akurasi_test.dma)<- c("SSE", "MSE", "MAPE")
colnames(akurasi_test.dma) <- c("Akurasi m = 4")
akurasi_test.dma
```

# Single Exponential Smoothing

## Manual

### Pemulusan

```{r}
ses.1 <- ses(train_ma.ts, h = 20, alpha = 0.7)
plot(ses.1)
```

```{r}
ses.1
```

```{r}
autoplot(ses.1) +
  autolayer(fitted(ses.1), series="Fitted") +
  ylab("Monorail User") + xlab("Periode")
```

### Evaluasi

```{r}
accuracy(ses.1, testing_ma$Monorail_User)
```

## Holt-Winter

### Pemulusan

```{r}
seshw<- HoltWinters(train_ma.ts, gamma = FALSE, beta = FALSE, alpha = 0.2)
plot(seshw)
```

```{r}
ramalanhw<- forecast(seshw, h=20)
ramalanhw
```

### Evaluasi

```{r}
accuracy(ramalanhw, testing_ma$Monorail_User)
```

## SES Optimum

### Pemulusan

```{r}
ses.opt <- ses(train_ma.ts, h = 20, alpha = NULL)
plot(ses.opt)
```

```{r}
ses.opt
```

### Evaluasi

```{r}
accuracy(ses.opt, testing_ma$Monorail_User)
```

# Double Exponential Smoothing

## Holt-Winters

### Pemulusan

```{r}
des<- HoltWinters(train_ma.ts, gamma = FALSE, beta = 0.3, alpha = 0.6)
plot(des)

```

```{r}
ramalandes<- forecast(des, h=20)
ramalandes
```

```{r}
plot(data.ts)
lines(des$fitted[,1], lty=2, col="blue")
lines(ramalandes$mean, col="red")
```

### Evaluasi

#### Data Latih

```{r}
ssedes.train<-des$SSE
msedes.train<-ssedes.train/length(train_ma.ts)
sisaandes<-ramalandes$residuals
head(sisaandes)
```

```{r}
mapedes.train <- sum(abs(sisaandes[3:length(train_ma.ts)]/train_ma.ts[3:length(train_ma.ts)])
                      *100)/length(train_ma.ts)

akurasides <- matrix(c(ssedes.train,msedes.train,mapedes.train))
row.names(akurasides)<- c("SSE", "MSE", "MAPE")
colnames(akurasides) <- c("Akurasi lamda=0.6 dan gamma=0.3")
akurasides

```

#### Data Uji

```{r}
selisihdes<-ramalandes$mean-testing_ma$Monorail_User
selisihdes
```

```{r}
SSEtestingdes<-sum(selisihdes^2)
MSEtestingdes<-SSEtestingdes/length(testing_ma$Monorail_User)
MAPEtestingdes<-sum(abs(selisihdes/testing_ma$Monorail_User)*100)/length(testing_ma$Monorail_User)

akurasidestest <- matrix(c(SSEtestingdes,MSEtestingdes,MAPEtestingdes))
row.names(akurasidestest)<- c("SSE", "MSE", "MAPE")
colnames(akurasidestest) <- c("Akurasi lamda=0.6 dan gamma=0.3")
akurasidestest
```

## DES Optimum

### Pemulusan

```{r}
des.opt<- HoltWinters(train_ma.ts, gamma = FALSE)
des.opt
```

```{r}
plot(des.opt)
```

```{r}
ramalandesopt<- forecast(des.opt, h=20)
ramalandesopt
```

### Evaluasi

#### Data Latih

```{r}
ssedesopt.train<-des.opt$SSE
msedesopt.train<-ssedesopt.train/length(train_ma.ts)
sisaandesopt<-ramalandesopt$residuals
head(sisaandesopt)
```

```{r}
mapedesopt.train <- sum(abs(sisaandesopt[3:length(train_ma.ts)]/train_ma.ts[3:length(train_ma.ts)])
                      *100)/length(train_ma.ts)

akurasidesopt <- matrix(c(ssedesopt.train,msedesopt.train,mapedesopt.train))
row.names(akurasidesopt)<- c("SSE", "MSE", "MAPE")
akurasidesopt

```

#### Data Uji

```{r}
selisihdesopt<-ramalandesopt$mean-testing_ma$Monorail_User
selisihdesopt

SSEtestingdesopt<-sum(selisihdesopt^2)
MSEtestingdesopt<-SSEtestingdesopt/length(testing_ma$Monorail_User)
MAPEtestingdesopt<-sum(abs(selisihdesopt/testing_ma$Monorail_User)*100)/length(testing_ma$Monorail_User)

akurasidestestopt <- matrix(c(SSEtestingdesopt,MSEtestingdesopt,MAPEtestingdesopt))
row.names(akurasidestestopt)<- c("SSE", "MSE", "MAPE")
akurasidestestopt
```

# Evaluasi Metrik

## Ringkasan MAPE

- **Single Moving Average (m=4):** Menghasilkan nilai MAPE sebesar 10,82% pada data latih dan 15,42% pada data uji

- **Double Moving Average (m=4):** Menghasilkan nilai MAPE sebesar 14,02% pada data latih dan 26,07% pada data uji

- **Single Exponential Smoothing:\
  SES Manual (α=0,7):** Menghasilkan nilai MAPE sebesar 9,78% pada data latih dan 13,24% pada data uji\
  **SES Holt-Winter (α=0,2):** Menghasilkan nilai MAPE sebesar 10,03% pada data latih dan 14,19% pada data uji\
  **SES Optimum:** Menghasilkan nilai MAPE sebesar 7,96% pada data latih dan 13,72% pada data uji

- **Double Exponential Smoothing:\
  DES Holt-Winter (α=0,6 ; β=0,3):** Menghasilkan nilai MAPE sebesar 11,20% pada data latih dan 26,03% pada data uji\
  **DES Optimum:** Menghasilkan nilai MAPE sebesar 10,80% pada data latih dan 20,18% pada data uji\

## Kesimpulan

Berdasarkan hasil evaluasi, **Single Exponential Smoothing (SES) dengan parameter** $\alpha = 0,7$ merupakan metode terbaik untuk memodelkan pergerakan pengguna *monorail*. Hal ini dikarenakan metode tersebut menghasilkan tingkat kesalahan peramalan (MAPE) paling kecil pada **data uji** dibandingkan metode lainnya, yakni sebesar 13,24%.
